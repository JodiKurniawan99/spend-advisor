enum DecisionCategory { electronics, groceries, lifestyle, health, education, other }

enum PurchaseType { need, want }

enum PaymentType { cash, installment }

enum DecisionStatus { safe, consider, risky }
